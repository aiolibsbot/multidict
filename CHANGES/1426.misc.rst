Changed the pure-Python implementation to mark a temporarily removed hash
table entry by flipping the high bit of its hash instead of overwriting it
with a sentinel value, so restoring the entry no longer recomputes the hash
-- by :user:`asvetlov`
